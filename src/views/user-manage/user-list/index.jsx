import React, { memo } from 'react'
import { ListWrapper } from './style'

const UserList = memo(() => {
	return <ListWrapper>UserList</ListWrapper>
})

UserList.displayName = 'UserList'

export default UserList
