import React, { memo } from 'react'
import { ListWrapper } from './style'

const NewsList = memo(() => {
	return <ListWrapper>NewsList</ListWrapper>
})

NewsList.displayName = 'NewsList'

export default NewsList
