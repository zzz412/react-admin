import React, { memo } from 'react'
import { RoleWrapper } from './style'

const RoleList = memo(() => {
	return <RoleWrapper>RoleList</RoleWrapper>
})

RoleList.displayName = 'RoleList'

export default RoleList
