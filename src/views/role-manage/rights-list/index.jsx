import React, { memo } from 'react'
import { RightsWrapper } from './style'

const RightsList = memo(() => {
	return <RightsWrapper>RightsList</RightsWrapper>
})

RightsList.displayName = 'RightsList'

export default RightsList
